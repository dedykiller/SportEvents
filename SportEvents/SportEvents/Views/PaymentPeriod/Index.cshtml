@model SportEvents.ViewModels.PaymentPeriodCollectionVM

@{
    ViewBag.Title = "Seznam zúčtovacích období";
}

<a class="return" onclick="history.back()">
    Seznam zúčtovacích období
</a>
<hr />

    <section id="left">

        <p class="button-link">
            @Html.ActionLink("Přidat nadcházející období", "CreateNextPaymentPeriod", "PaymentPeriod", new { groupId = Model.PaymentPeriods.First().GroupId }, null)
        </p>
        <p class="button-link">
            @Html.ActionLink("Editace aktuálního období", "Edit", "PaymentPeriod", new { groupId = Model.PaymentPeriods.First().GroupId }, null)
        </p>
        <table class="table">
            <tr>
                <th>
                    <h3>Seznam zúčtovacích období</h3>
                </th>
                <th>
                    
                </th>
                <th>

                </th>
                <th>

                </th>


            </tr>
            @foreach (var item in Model.PaymentPeriods)
            {
                <tr onclick="location.href = '@(Url.Action("ListOfPayments", "PaymentPeriod", new { PaymentPeriodId = item.Id, groupId = item.GroupId }))'">
                    <td>
                        @Html.DisplayFor(modelItem => item.Start)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.End)
                    </td>

                </tr>
            }
        </table>


    </section>
